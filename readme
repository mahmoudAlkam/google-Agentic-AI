
## 🧩 1. Agentenentwicklung (angepasst für PDF-only Use Case)

### 🔹 Empfangsagent – Intent Detection via Dialogflow CX

**Ziel:**  
Erkennung des Kundenanliegens beim Erstkontakt über CES (Chat oder Voice).

**Umsetzungsschritte:**
1. Erstelle einen Agent in **Dialogflow CX**.
2. Definiere Intents:
   - Produktfrage
   - Garantieanfrage
   - Fehlercode
   - Unklarer Intent
3. Konfiguriere Entity-Erkennung:
   - `@Produkttyp`
   - `@Seriennummer`
   - `@Fehlercode`
4. Route basierend auf Intent zu den jeweiligen Spezialagenten.

---

### 🔹 Informationsagent – Zugriff auf PDF via RAG

**Ziel:**  
Beantwortung von Produkt- und Garantiefragen durch KI mit PDF als Datenquelle.

**Umsetzungsschritte:**
1. Verwende einen **PDF-Parser** (z. B. LangChain + PyMuPDF oder PDFMiner).
2. Implementiere **Retrieval-Augmented Generation (RAG)**:
   - Extrahiere relevante Abschnitte aus PDF basierend auf Anfrage.
   - Übergib die extrahierten Inhalte an ein generatives KI-Modell (z. B. Vertex AI).
3. Generiere Antwort inkl. Quellenverweis (z. B. Seitenzahl).
4. Berechne Confidence Score (siehe unten).
5. Bei Unsicherheit → `${FLOW:Rückfragen}` → ggf. `${FLOW:Eskalation}`

---

### 🔹 Analyseagent – Fehlercode-Analyse mit Regelwerk

**Ziel:**  
Technische Analyse von Fehlercodes auf Basis von PDF-Inhalten.

**Umsetzungsschritte:**
1. Definiere Mapping-Regeln: z. B. `E02` → „Wasserzulauf prüfen“.
2. Suche im PDF nach Fehlercode-Abschnitten.
3. Extrahiere Lösungsvorschläge.
4. Generiere Antwort mit KI.
5. Confidence Score prüfen → ggf. Rückfrage oder Eskalation.

---

### 🔹 Eskalationsagent – Schwellenwertlogik + Jira-Ticket-Erstellung

**Ziel:**  
Übergabe komplexer Fälle an menschliche Agenten.

**Umsetzungsschritte:**
1. Definiere Schwellenwert für Confidence Score (z. B. `< 0.6`).
2. Bei Trigger → erst `${FLOW:Rückfragen}`, dann `${FLOW:Eskalation}`.
3. Erstelle Jira-Ticket via API mit:
   - Chatverlauf
   - Anfrageparameter
   - PDF-Auszüge
   - Confidence Score

---

### 🔹 Assistenzagent – Vorschläge & Suche für menschliche Agenten

**Ziel:**  
Unterstützung menschlicher Agenten mit PDF-Suche und Vorschlägen.

**Umsetzungsschritte:**
1. Implementiere semantische Suche in PDF.
2. Generiere Vorschläge basierend auf ähnlichen Fällen.
3. Ermögliche manuelle Ticket-Erstellung bei fehlenden Informationen.

---

### 🔹 Lernagent – Feedbackauswertung (ohne Confluence)

**Ziel:**  
Systemverbesserung durch Feedbackanalyse.

**Umsetzungsschritte:**
1. Analysiere Eskalationen und negatives Feedback.
2. Markiere PDF-Stellen, die unklar oder unvollständig sind.
3. Erstelle Vorschläge zur PDF-Ergänzung (z. B. neue Version).
4. Übergabe an Content-Team zur PDF-Pflege.

---

## 🧩 2. Flows & Routing (angepasst)

### 🔹 Flows in CES definieren

| Flow            | Zweck                                      |
|-----------------|---------------------------------------------|
| `${FLOW:Begrüßung}` | Begrüßung, Intent-Erkennung starten         |
| `${FLOW:Rückfragen}` | Klärung bei Unsicherheit oder fehlenden Parametern |
| `${FLOW:Eskalation}` | Übergabe an menschlichen Agenten via Jira |
| Monitoring       | Logging für Lernagent und Reporting        |

---

### 🔹 Routing-Logik gemäß Intent & Confidence Score

| Intent              | Agent              | Bei Unsicherheit → Rückfragen | Bei Scheitern → Eskalation |
|---------------------|--------------------|-------------------------------|-----------------------------|
| Produktfrage        | Informationsagent  | ✅                             | ✅                           |
| Garantieanfrage     | Informationsagent  | ✅                             | ✅                           |
| Fehlercode          | Analyseagent       | ✅                             | ✅                           |
| Unklarer Intent     | Empfangsagent      | —                              | —                            |

---

## 🧠 Confidence Score berechnen (PDF-only)

**Empfohlene Methode:**  
Verwende semantische Ähnlichkeit zwischen Anfrage und extrahiertem PDF-Abschnitt.

```python
from sklearn.metrics.pairwise import cosine_similarity

score = cosine_similarity([frage_embedding], [pdf_abschnitt_embedding])[0][0]
```

- `< 0.6` → Rückfrage
- `< 0.4` nach Rückfrage → Eskalation

---

Wenn du möchtest, kann ich dir ein Beispielskript für die PDF-Suche, Confidence Score Berechnung oder Jira-Integration erstellen. Sag einfach Bescheid!
